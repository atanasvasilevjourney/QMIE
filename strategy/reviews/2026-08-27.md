# QMIE review — 2026-08-27

verdict: success
proposed_knob: scan_timeframes
proposed_from: 1h,4h
proposed_to: 4h
applied: false
catalog_next: sig_min_adx

This file is a proposal. Do **not** auto-write `.env`. Change one live
knob only after a human applies it.

`python -m improve.review` (2026-08-27 11:35 UTC) wrote
`strategy/reviews/2026-08-27-113540.md` with `alloc_top_long` 3 → 2
because `scan_timeframes` is already in `strategy/reviews/2026-08-17.md`
and `strategy/reviews/2026-08-25.md` (`applied: false`), and a prior
same-day draft had already listed `sig_min_adx`. That is the catalog
doing its job. **This cycle does not apply ADX or book-size knobs.**
One variable is still outstanding: `SCAN_TIMEFRAMES` `1h,4h` → `4h`.
Catalog next after that knob is live and measured is `sig_min_adx`
0.0 → 20.0. Do not change both. Do not add `1d` to `SCAN_TIMEFRAMES`.
Do not re-apply KovaView skip gates (`too_late`, BTC-RED `buys_allowed`,
cooldown).

## Goal

- Success: win% ≥ 48.0 · expectancy R ≥ 0.15 · Sharpe ≥ 1.0 (backtest) ·
  max DD ≤ 20.0%
- Failure: win% < 40.0 or expectancy R < 0.0 after 30 closed A/A+ fills

## Live 6-agent briefing (`GET /health`, `GET /paper`, `GET /agents/briefing`)

Read-only. `places_orders: false` on every agent and on `/paper`.

`:8080` health: `status=ok`, `data_source=okx`, paper enabled, no orders,
uptime ~55618s, scanner 14 passes / 13 alerts / 0 errors, radar 2 passes /
8 errors. `:8081` health also `ok` (uptime ~14258s, radar_errors 0).

| Agent | Headline |
|---|---|
| radar | breadth LONG · G23 Y3 R0 (88.5% green, BTC GREEN, `status: incomplete`) |
| scanner | 24 A/A+ of 50 recent alerts (3 A+ / 21 A; TFs 1h 34 · 4h 6 · 1d 10) |
| checklist | 0 GO · 7 WATCH · 1 SKIP |
| book | 1 slot · mode ranked · `weight_pct` is a risk budget (`GET /agents/desk` `places_orders: false`) |
| review | live endpoint still read the same-day catalog draft (`sig_min_adx`); outstanding knob remains `scan_timeframes` · applied false · journal success |
| analysis | Template overlay (no `OPENAI_API_KEY`) |

Scanner note on the briefing: 1h dilutes frozen OOS; prefer 4h A/A+ when
both fire. Radar `status: incomplete` on `:8080` (OKX daily feed in this VM).

Paper (`GET /paper` on both :8080 and :8081): 97 fills, 35 open, 62 closed,
closed_pnl ~915.42, `places_orders: false`.

## Journal (A/A+ fills)

- db: `python/data/qmie.db` (`cd python` default `data/qmie.db`)
- closed: 68  wins: 39  losses: 29  win%: 57.4
- avg realized R: 0.511
- CLI verdict `success` is **journal math**, not a clean 30-fill manual
  live book:
  - source: **62 paper / 6 manual** among 68 closed
  - notes on closed rows: 63 `paper auto-fill`, plus hello-world / qa
    tags on the remaining manual rows
  - TF mix of closed A/A+: **60× 1h, 8× 4h** — the drag timeframe
    dominates the journal, so win 57.4% cannot be used to freeze edge
  - closed **manual** 4h A/A+: **5** (need 30)
- Need 30 closed **manual** 4h A/A+ fills before treating live journal
  as the measurement. Frozen OOS stays the edge table.

## Frozen OOS vs goals

Numbers from [`docs/backtest-baseline.md`](../../docs/backtest-baseline.md)
(TMA 9/90/199, ATR% 0.4–4.0 then ADX ≥ 20, split 2025-01-01). Not re-run
this cycle.

| Slice | Closed | Win % | E[R] | PF | Sharpe |
|---|---:|---:|---:|---:|---:|
| Combined 1h+4h A/A+ OOS | 12744 | 42.1% | +0.122 | 1.21 | 1.30 |
| **4h A/A+ OOS** | **2152** | **49.1%** | **+0.309** | **1.61** | **2.09** |
| 1h A/A+ OOS | 10592 | 40.6% | +0.084 | 1.14 | 0.89 |
| Combined C OOS | 23860 | 39.3% | +0.047 | 1.08 | 0.99 |
| Combined A/A+ IS | 6775 | 40.1% | +0.070 | 1.12 | 0.96 |

Vs goals on **combined** OOS: win 42.1 < 48.0 · E[R] +0.122 < 0.15 ·
Sharpe 1.30 ≥ 1.0 · max DD is in R units, not account %. **4h-only OOS
clears** win / E[R] / Sharpe. Combined PF 1.21 still misses Sprint 1’s
1.3 bar because 1h is ~5× the 4h sample.

## Baseline knobs

- `alloc_mode`: ranked
- `alloc_top_long`: 3
- `alloc_top_short`: 3
- `alloc_min_grade`: A
- `alloc_weighting`: rank
- `alloc_cluster_max`: 1
- `alloc_norm_length`: 20
- `alloc_norm_threshold`: 0.0
- `alloc_ma_filter`: False
- `alloc_dual`: False
- `alloc_defensive2`: cash
- `scan_timeframes`: 1h,4h
- `scan_min_alert_grade`: A
- `sig_min_adx`: 0.0
- `sig_min_atr_pct`: 0.1
- `sig_max_atr_pct`: 8.0
- `sig_funding_rate_threshold`: 0.001

## Hypothesis

Change **only** `scan_timeframes` / `SCAN_TIMEFRAMES` from `1h,4h` to
`4h` (same proposal as 2026-08-17 and 2026-08-25; still unapplied).

1h volume pulls pooled PF under 1.3. 4h A/A+ already has PF 1.61 and
win 49.1% on the frozen OOS. Daily Trend Radar stays independent of
`SCAN_TIMEFRAMES`. HTF for 4h remains 1D via `SCAN_HTF_MAP`.

Do **not** also set `sig_min_adx` 20, drop `alloc_top_long` to 2, or
tighten the ATR band this cycle. Do not retune `W_*`. Do not change
EMA 9/90/199. Do not apply `SCAN_TIMEFRAMES=4h` from this review file
without a human `.env` + `baseline.yaml` edit.

How we will know it worked (next cycle):

- **Toward goal:** ≥ 30 closed **manual** 4h A/A+ fills move toward
  win% ≥ 48.0 and E[R] ≥ 0.15, or a reprint of the frozen parquet
  restricted to `timeframe=4h` stays near PF ≥ 1.3.
- **Toward failure:** 4h A/A+ E[R] ≤ 0 or win% < 40.0. Revert to
  `1h,4h`. Next catalog knob after a successful 4h-only cycle is
  `sig_min_adx` 0 → 20.

## Out of scope

- No broker execution, Hermes, Signum, HyperLiquid, or Railway.
- No TradingView MCP numbers in `W_*` or `compute_signal`.
- OpenAI Take is not a score (`GET /agents/analysis/{id}` template).
- Do not treat paper / hello-world / qa PnL as live edge.
- Pine / `signal_engine.py` untouched this cycle.
- No `.env` write, no orders.

Companion (same day): Trend Radar / Daily breakout / paper R math and
desk presentation —
[`2026-08-27-radar-breakout-execution.md`](2026-08-27-radar-breakout-execution.md).
Does not propose a second live knob.
