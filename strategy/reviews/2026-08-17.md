# QMIE review — 2026-08-17

verdict: short_of_goal
proposed_knob: scan_timeframes
proposed_from: 1h,4h
proposed_to: 4h
applied: false

This file is a proposal. Do **not** auto-write `.env`.
Change one live knob only after a human applies it. If applied, update
`strategy/baseline.yaml` in the same change (`scan_timeframes: 4h`).

Evidence is the frozen TMA walk-forward in
[`docs/backtest-baseline.md`](../../docs/backtest-baseline.md), not the
fill journal.

## Goal

- Success: win% ≥ 48 · expectancy R ≥ 0.15 · Sharpe ≥ 1.0 (backtest) ·
  max DD ≤ 20%
- Failure: win% < 40 or expectancy R < 0 after 30 closed A/A+ fills

## Journal (A/A+ fills)

- closed: 0  (below `review.min_closed_fills: 30`)
- This cycle does **not** wait on the journal. Sprint 1 OOS is the
  measurement. Keep journaling manual entries against `GET /signals`
  ids so the next weekly loop can run.

## Baseline knobs (live, unchanged)

- `scan_timeframes`: 1h,4h
- `scan_min_alert_grade`: A
- `sig_min_adx`: 0.0
- `sig_min_atr_pct`: 0.10
- `sig_max_atr_pct`: 8.0
- `alloc_mode`: ranked
- `alloc_top_long` / `alloc_top_short`: 3 / 3

## Frozen OOS (TMA 9/90/199, ADX≥20, ATR% 0.4–4.0, split 2025-01-01)

| Slice | A/A+ closed | Win % | E[R] | PF | Sharpe |
|---|---:|---:|---:|---:|---:|
| Combined 1h+4h | 12744 | 42.1% | +0.122 | 1.21 | 1.30 |
| **4h only** | **2152** | **49.1%** | **+0.309** | **1.61** | **2.09** |
| 1h only | 10592 | 40.6% | +0.084 | 1.14 | 0.89 |
| Combined C | 23860 | 39.3% | +0.047 | 1.08 | 0.99 |

Combined A/A+ beats C and has E[R] > 0, but PF 1.21 < 1.3 and win%
42.1 < 48. **1h is the drag** (~5× the 4h sample). 4h A/A+ clears the
Sprint 1 gate and the numeric goals.

A+ OOS (E[R] +0.066, PF 1.11) is worse than A (E[R] +0.147, PF 1.26).
Do not raise `SCAN_MIN_ALERT_GRADE` to `A+`.

## Hypothesis

Change **only** `scan_timeframes` / `SCAN_TIMEFRAMES` from `1h,4h` to
`4h`.

1h alerts are the volume that pulls pooled PF under 1.3. 4h A/A+ already
has PF 1.61 and win 49.1% on the frozen OOS. Daily Trend Radar stays
independent of `SCAN_TIMEFRAMES` (1D RGG / coils). HTF for 4h remains
1D via `SCAN_HTF_MAP`.

Do **not** also change `sig_min_adx` or the ATR band in this cycle
(ADX≥20 helped A/A+ E[R] +0.094 → +0.122; smaller than the TF split).
Do not retune `W_*`. Do not change EMA 9/90/199.

How we will know it worked (next cycle, toward goal vs toward failure):

- **Toward goal:** live journaled 4h A/A+ fills (n ≥ 30) move toward
  win% ≥ 48 and E[R] ≥ 0.15, or a re-print of this same parquet
  restricted to `timeframe=4h` stays near PF ≥ 1.3.
- **Toward failure:** 4h A/A+ E[R] ≤ 0 or win% < 40. Revert to
  `1h,4h` and stop. Next catalog knob after a successful 4h-only
  cycle is `sig_min_adx` 0 → 20.

## Out of scope

- No broker execution, Hermes, Signum, HyperLiquid, or Railway.
- No TradingView MCP numbers in `W_*` or `compute_signal`.
- No TQQQ notebook periods, no Supertrend restore, no extra score votes.
- Pine / `signal_engine.py` untouched this cycle (math already TMA).
