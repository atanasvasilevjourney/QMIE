# QMIE review — 2026-08-25

verdict: success
proposed_knob: scan_timeframes
proposed_from: 1h,4h
proposed_to: 4h
applied: false
catalog_next: sig_min_adx

This file is a proposal. Do **not** auto-write `.env`. Change one live
knob only after a human applies it.

`python -m improve.review` wrote `sig_min_adx` 0 → 20 because
`scan_timeframes` is already in `strategy/reviews/2026-08-17.md`
(`applied: false`). That is the catalog doing its job. **This cycle does
not apply ADX.** One variable is still outstanding: `SCAN_TIMEFRAMES`
`1h,4h` → `4h`. Catalog next after that knob is live and measured is
`sig_min_adx` 0.0 → 20.0. Do not change both.

## Goal

- Success: win% ≥ 48.0 · expectancy R ≥ 0.15 · Sharpe ≥ 1.0 (backtest) ·
  max DD ≤ 20.0%
- Failure: win% < 40.0 or expectancy R < 0.0 after 30 closed A/A+ fills

## Live 6-agent briefing (`GET /agents/briefing`)

Read-only. `places_orders: false` on every agent and on `/paper`.

| Agent | Headline |
|---|---|
| radar | breadth LONG · G22 Y4 R0 (84.6% green, BTC GREEN) |
| scanner | 19 A/A+ of 50 recent alerts (4 A+ / 15 A; TFs 1h 41 · 4h 2 · 1d 7) |
| checklist | 0 GO · 8 WATCH · 0 SKIP |
| book | 0 slots · mode ranked · `quantity` always 0 (`GET /agents/desk`) |
| review | knob `scan_timeframes` · applied false · journal success |
| analysis | Template overlay (no `OPENAI_API_KEY`) |

Scanner note on the briefing: 1h dilutes frozen OOS; prefer 4h A/A+ when
both fire. Radar `status: incomplete` (OKX daily feed in this VM).

Health: `GET /health` 200, `data_source=okx`, paper enabled, no orders.

## Journal (A/A+ fills)

- db: `data/qmie.db`
- closed: 56  wins: 36  losses: 20  win%: 64.3
- avg realized R: 0.691
- CLI verdict `success` is **journal math**, not a clean 30-fill manual
  live book:
  - source: **50 paper / 6 manual** among 56 closed
  - notes on closed rows: 51 `paper auto-fill`, plus hello-world / qa
    tags on most of the remaining 5
  - TF mix of closed A/A+: **49× 1h, 7× 4h** — the drag timeframe
    dominates the journal, so win 64.3% cannot be used to freeze edge
- Paper book (`GET /paper`): 76 fills, 26 open, 50 closed, closed_pnl
  ~1127.9, `places_orders: false`
- Need 30 closed **manual** 4h A/A+ fills before treating live journal
  as the measurement. Frozen OOS stays the edge table.

## Tests

- `cd python && pytest -v` → **316 passed**, 2 scheduler
  `RuntimeWarning` (un-awaited AsyncMock on funding in two daily-df
  routing tests). 2.28s.
- `cd web && npm run lint` (oxlint) → clean.

## Walk-forward backtest (re-run 2026-08-25)

```bash
cd python
python -m backtest.run \
  --start 2024-01-01 --split 2025-01-01 --end 2026-08-24 \
  --min-adx 20 --min-atr-pct 0.4 --max-atr-pct 4.0
```

Vision cache, not live `fapi`. Runtime ~1974s. Raw 137,592 → ATR
132,989 → ADX 96,146. Timestamp span in parquet: 2024-01-13 →
2026-07-31 (Vision months; `--end 2026-08-24` did not add August 2026
bars).

Numbers **match** [`docs/backtest-baseline.md`](../../docs/backtest-baseline.md)
bit-for-bit on the gated tables. That file is **not** overwritten.

| Slice | Closed | Win % | E[R] | PF | Sharpe |
|---|---:|---:|---:|---:|---:|
| Combined 1h+4h A/A+ OOS | 12744 | 42.1% | +0.122 | 1.21 | 1.30 |
| **4h A/A+ OOS** | **2152** | **49.1%** | **+0.309** | **1.61** | **2.09** |
| 1h A/A+ OOS | 10592 | 40.6% | +0.084 | 1.14 | 0.89 |
| Combined C OOS | 23860 | 39.3% | +0.047 | 1.08 | 0.99 |
| Combined A/A+ IS | 6775 | 40.1% | +0.070 | 1.12 | 0.96 |

Vs goals on **combined** OOS: win 42.1 < 48 · E[R] +0.122 < 0.15 ·
Sharpe 1.30 ≥ 1.0. **4h-only OOS clears** win / E[R] / Sharpe. Combined
PF 1.21 still misses Sprint 1’s 1.3 bar because 1h is ~5× the 4h sample.

Gate ablation (A/A+ OOS) is unchanged: ungated E[R] +0.094 → ATR-only
+0.103 → ADX-only +0.110 → ATR+ADX +0.122. ADX helps; dropping 1h helps
more.

## Baseline knobs

- `alloc_mode`: ranked
- `alloc_top_long`: 3
- `alloc_top_short`: 3
- `alloc_min_grade`: A
- `alloc_weighting`: rank
- `alloc_cluster_max`: 1
- `alloc_norm_length`: 20
- `alloc_norm_threshold`: 0.0
- `alloc_ma_filter`: False
- `alloc_dual`: False
- `alloc_defensive2`: cash
- `scan_timeframes`: 1h,4h
- `scan_min_alert_grade`: A
- `sig_min_adx`: 0.0
- `sig_min_atr_pct`: 0.1
- `sig_max_atr_pct`: 8.0
- `sig_funding_rate_threshold`: 0.001

## Hypothesis

Change **only** `scan_timeframes` / `SCAN_TIMEFRAMES` from `1h,4h` to
`4h` (same proposal as 2026-08-17; still unapplied).

1h volume pulls pooled PF under 1.3. 4h A/A+ already has PF 1.61 and
win 49.1% on the frozen (and now reprinted) OOS. Daily Trend Radar stays
independent of `SCAN_TIMEFRAMES`. HTF for 4h remains 1D via
`SCAN_HTF_MAP`.

Do **not** also set `sig_min_adx` 20 or tighten the ATR band this
cycle. Do not retune `W_*`. Do not change EMA 9/90/199.

How we will know it worked (next cycle):

- **Toward goal:** ≥ 30 closed **manual** 4h A/A+ fills move toward
  win% ≥ 48 and E[R] ≥ 0.15, or a reprint of this parquet restricted to
  `timeframe=4h` stays near PF ≥ 1.3.
- **Toward failure:** 4h A/A+ E[R] ≤ 0 or win% < 40. Revert to `1h,4h`.
  Next catalog knob after a successful 4h-only cycle is `sig_min_adx`
  0 → 20.

## Out of scope

- No broker execution, Hermes, Signum, HyperLiquid, or Railway.
- No TradingView MCP numbers in `W_*` or `compute_signal`.
- OpenAI Take is not a score (`GET /agents/analysis/{id}` template).
- Do not treat paper / hello-world / qa PnL as live edge.
- Pine / `signal_engine.py` untouched this cycle.
