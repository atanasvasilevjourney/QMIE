# QMIE — runtime requirements (Scanner Edition)
# Python 3.12+

fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2

aiosqlite==0.20.0
aiohttp==3.10.10

# Numerics — scanner math
numpy==2.1.2
pandas==2.2.3

# Optional: Redis-backed dedup (recommended for multi-process / restarts)
redis==5.0.8

python-multipart==0.0.12
