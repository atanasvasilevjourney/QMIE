name: tests

on:
  push:
    branches: [main, master, develop]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install runtime + test deps
        working-directory: python
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          # Backtest unit tests import requests + pyarrow. Do NOT install
          # backtest/requirements.txt wholesale: streamlit==1.35.0 pins
          # numpy<2 and conflicts with runtime numpy==2.1.2.
          pip install requests==2.32.3 pyarrow==16.1.0
          pip install pytest pytest-asyncio pytest-cov

      - name: Run tests
        working-directory: python
        env:
          WEBHOOK_SECRET: ci-test-secret
          DISCORD_ENABLED: "false"
          TELEGRAM_ENABLED: "false"
        run: pytest -v --cov=. --cov-report=term-missing --cov-report=xml

      - name: Upload coverage
        if: matrix.python-version == '3.12'
        uses: actions/upload-artifact@v4
        with:
          name: coverage-xml
          path: python/coverage.xml
